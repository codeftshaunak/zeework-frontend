import React from "react";

const ProfileContainer = ({ children }) => {
  return <div className="mx-auto py-8 w-full max-w-[1400px] px-4 lg:px-6 xl:px-8">{children}</div>;
};

export default ProfileContainer;
